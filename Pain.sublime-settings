{
    // {String} "directional" moves the separator in the arrow's direction;
    // "growth" grows or shrinks the active pane
    "resize_mode": "directional",
    // {Boolean} if true, then adjust adjacent panes during resize
    "greedy_pane": false,
    // {Integer} the amount to resize pane in the range of 1 to 100
    "resize_amount": 3
}
